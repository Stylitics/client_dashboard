<%= f.input :name %>

<ul id="r-script-sources">
  <%= f.fields_for :sources do |s| %>
    <%= render 'source_fields', f: s %>
  <% end %>
</ul>
<%= link_to_add_fields 'Add new source', f, :sources %>
