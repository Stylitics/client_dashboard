<% if @chart.is_running == true %>
  setTimeout(function() {
    return $.get("/charts/trend-line/chart_runs/" + $("#trend-chart-filter-form").data("id") + "/check");
  }, 10000);
<% else %>
  window.location.reload();
  // $("#my-saved-segments").click();
  // window.trend_line_chart.readJSON();
<% end %>